# Run the gauge-ts runner through pnpm instead of letting the launcher resolve
# tsx itself. `tsx` must be a direct dependency of this project for this to
# work: pnpm only links direct dependencies into node_modules/.bin.
GAUGE_TS_PACKAGE_RUNNER = pnpm
